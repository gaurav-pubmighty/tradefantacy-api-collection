info:
  name: Auth Validate
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/auth/validate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
