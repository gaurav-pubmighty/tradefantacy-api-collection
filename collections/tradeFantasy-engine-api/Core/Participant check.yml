info:
  name: Participant check
  type: http
  seq: 2

http:
  method: POST
  url: "{{core-base-url}}/check-participant"
  body:
    type: json
    data: |-
      {
        "contestId": 21,
        "userRef": 27
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
