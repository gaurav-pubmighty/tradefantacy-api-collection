info:
  name: Market
  type: websocket
  seq: 1

websocket:
  url: "{{wsUrl}}/market"
  message:
    - title: message 1
      selected: false
      message:
        type: json
        data: |-
          {
            "action": "subscribe",
            "topic": "contest:subscribe",
            "contestId": "21"
          }
  auth:
    type: bearer
    token: "{{user-token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
