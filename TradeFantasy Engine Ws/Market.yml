info:
  name: Market
  type: websocket
  seq: 1

websocket:
  url: "{{wsUrl}}/market"
  message:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{user-token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
