info:
  name: Admin
  type: websocket
  seq: 2

websocket:
  url: "{{wsUrl}}/admin"
  message:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{admin-token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
