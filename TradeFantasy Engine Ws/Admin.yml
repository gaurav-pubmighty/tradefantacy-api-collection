info:
  name: Admin
  type: websocket
  seq: 2

websocket:
  url: "{{wsUrl}}/admin"
  message:
    - title: message 1
      selected: false
      message:
        type: json
        data: |-
          {
            "action": "subscribe",
            "topic": "contest:subscribe",
            "contestId": 21
          }
  auth:
    type: bearer
    token: "{{admin-token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
