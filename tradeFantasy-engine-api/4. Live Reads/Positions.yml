info:
  name: Positions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/user/contest/{{contestId}}/positions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
