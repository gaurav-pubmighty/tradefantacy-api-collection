info:
  name: Price (all symbols)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/user/price"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
