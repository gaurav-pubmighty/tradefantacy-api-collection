info:
  name: Join Contest
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/user/contest/join"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "contestId": "{{contestId}}",
        "ssvToken": "{{ssvToken}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: "const baseUrl = bru.getCollectionVar(\"baseUrl\");\r

        const token = bru.getCollectionVar(\"token\");\r

        \r

        await bru.sendRequest({\r

        \    url: `${baseUrl}/user/ssv/issue`, // change API path if different\r

        \    method: \"GET\",\r

        \    headers: {\r

        \        \"Content-Type\": \"application/json\",\r

        \        \"Authorization\": `Bearer ${token}`\r

        \    }\r

        }, async function(err, res) {\r

        \    if (err) {\r

        \        console.log(\"SSV token request error:\", err);\r

        \        return;\r

        \    }\r

        \r

        \    const j = res.data;\r

        \r

        \    if (j.success && j.data && j.data.ssvToken) {\r

        \        pm.collectionVariables.set(\"ssvToken\", j.data.ssvToken);\r

        \        console.log(\"ssvToken saved:\", j.data.ssvToken);\r

        \    } else {\r

        \        console.log(\"ssvToken not found:\", j);\r

        \    }\r

        });"
    - type: after-response
      code: "test('200 OK', () => expect(res.getStatus()).to.equal(200));\r

        test('Has wallet', () => {\r

        \  var d = res.getBody().data;\r

        \  expect(d).to.have.property('wallet');\r

        });"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
