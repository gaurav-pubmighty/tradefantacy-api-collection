info:
  name: Quote route root
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/quote/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
