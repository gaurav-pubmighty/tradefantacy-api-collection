info:
  name: Admin route root
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/admin/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
